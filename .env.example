# ================================
# Sona Provisioning Frontend
# Environment Variables Template
# ================================

# Port Configuration
# ------------------
# The port on which the frontend container will be exposed to the VPS host machine.
# Your VPS nginx reverse proxy should forward requests to localhost:${PROVISIONING_FRONTEND_PORT}
# Default: 3000
PROVISIONING_FRONTEND_PORT=3000

# Backend API URL
# ---------------
# URL for the backend API service (browser-accessible)
#
# For Docker container-to-container communication, use the backend container name:
# Format: http://<backend-container-name>:<backend-port>
# Example: http://sona-backend:3333
#
# To find your backend container name, run:
# docker ps --format "table {{.Names}}\t{{.Ports}}"
NEXT_PUBLIC_API_URL=http://sona-backend:3333

# Node Environment
# ----------------
# Should be "production" for VPS deployment
NODE_ENV=production

# Optional: Internal Container Port
# ----------------------------------
# The port Next.js listens on inside the container (should match EXPOSE in Dockerfile)
# Usually you don't need to change this unless you have specific requirements
# PORT=3000
